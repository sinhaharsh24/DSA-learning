public class timecomplextnotes {
    public static void main(String[] args) {
        //time complexity and time used by computer to done the computation is defferent
        //the comparision is like
        // O(1) < O(log N) < O(N) < O(N log N) < O(2^n)

    }
}
